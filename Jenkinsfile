pipeline {

    agent any

    stages {

        stage("build"){

            steps{

                echo "Hello Build"
            }
        }

        stage("test"){

            steps{

                echo "Hello test"
            }

        }

        stage("deploy"){

            steps{

                echo "Hello test"
            }                        
        }
    }
}


node {
    // groovy script
}